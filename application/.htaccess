<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # Redirect to the index.php file if the URL does not contain it
    RewriteCond %{REQUEST_FILENAME} !-f
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>